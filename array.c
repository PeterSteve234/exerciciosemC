int main () {
char nome[50];
printf("digite seu nome", nome);
scanf(%c", &nome);

  int i;
  int contador; 

for (i = 0; i < nome[1] != '\0'; ++i) {
(( nome[i] >= 'A' && nome[i] <= 'Z')) || ((  nome[i] >= 'a' &&  <= nome[i] <= 'z')) {
  contador++
 }
  printf("Numero de letras %d\n", contador);
}

}
